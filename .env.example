# ─── JWT ─────────────────────────────────────────────────────────
JWT_SECRET=schoolPortalSecretKey2026VeryLongAndSecure_ChangeThis!

# ─── EMAIL (Notification Service) ────────────────────────────────
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=kpmedicalshop0@gmail.com
# ✅ FIX: Value has spaces — must be quoted so shell doesn't split it
MAIL_PASS="cwlj kapx jmwj cvdq"

# # ─── AWS S3 (Media/File Storage) ─────────────────────────────────
# AWS_ACCESS_KEY=your-access-key
# AWS_SECRET_KEY=your-secret-key
# AWS_BUCKET_NAME=school-portal-media
# AWS_REGION=ap-south-1

# ─── Database (auto-configured in docker-compose) ─────────────────
# ✅ FIX: Was missing the # comment prefix — shell would error on this line
# Override here if using external databases
DB_USER=school_user
DB_PASS=school_pass
